[package]
name = "duck-hunt"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.15", features = ["dynamic_linking"] }

# Enable optimizations for dependencies in debug builds (keeps duck movement smooth)
[profile.dev.package."*"]
opt-level = 3

[profile.dev]
opt-level = 1
